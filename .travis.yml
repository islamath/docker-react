language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t medrahim/docker-react -f Dockefile.dev .

script:
  - docker run -e CI=true medrahim/docker-react npm run test